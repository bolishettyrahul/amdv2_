# Core runtime dependencies (CPU-only, sandbox-safe)
requests>=2.32
sympy>=1.13
vaderSentiment>=3.3.2
z3-solver>=4.13
# Stage 1 NER at $0 (the en_core_web_sm model is installed in the Dockerfile)
spacy>=3.8

# Development / test
pytest>=8.3
